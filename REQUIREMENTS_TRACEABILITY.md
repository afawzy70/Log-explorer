# Requirements Traceability

Every item in handover §34 maps to at least one owning phase. Claude Code updates **Status** and **Evidence** in every phase; Phase M cannot pass while any row is `Not assessed` or has an unexplained gap.

**Status values:** `Not assessed` · `Present` · `Partial` · `Missing` · `Broken` · `Done` · `Deferred (reason)`
**Evidence:** file path + line, test name, or report path. Never "assumed" or "looks fine".

| # | Requirement | Handover § | Owning phase | Status | Evidence |
|---:|---|---|---|---|---|
| 1 | Problem statement understood and preserved | 1–2 | A | Present | Understood and preserved in HANDOVER.md (read in full); see docs/AUDIT.md §5 row 1. |
| 2 | Two logical sources (Docker, OpenShift Loki) | 1 | C, D | Done | Docker half: source/docker/DockerLogSource.java (id="local-docker"), docs/verification/PHASE_C_REPORT.md. Loki half: source/loki/LokiLogSource.java (id="openshift-loki"), 55 Loki-specific tests + EventFiltersTest (shared), real end-to-end verification against a live backend + tools/mock-loki (docs/verification/PHASE_D_REPORT.md). Live real-cluster verification DEFERRED (no cluster reachable). |
| 3 | Portable Docker Compose delivery | 20 | K | Done | `docker-compose.yml` (`app` unprofiled/always-on, `demo-log-generator` under `demo`, `mock-loki` under `loki-mock`) + `docker-compose.docker-socket.yml` override. All three profiles built, started, and exercised for real against this repo's own real Docker daemon this phase (not assumed) - see `docs/verification/PHASE_K_REPORT.md` for the full session log incl. two real bugs found and fixed along the way. `scripts/smoke.sh` run for real, passing. |
| 4 | Remote Docker default port prefilled | 11, 20.2 | C | Done | DockerProperties#port defaults to 2375; DockerClientFactory#buildConfig builds the REMOTE tcp:// URI from it. DockerClientFactoryTest#remoteModeUsesTheDefaultPortWhenNotOverridden. |
| 5 | Remote Docker custom port supported | 11, 24.3 | C | Done | DockerClientFactoryTest#remoteModeHonorsAnOverriddenPort - port explicitly overridden to 9999, resulting Docker host URI reflects it. |
| 6 | Remote Docker TLS optional | 11.1, 24.2 | C | Done | DockerClientFactory#buildConfig: TLS only set from properties.isTls(); DockerClientFactoryTest covers TLS off (no cert required) and TLS on (valid cert directory required, else IllegalStateException - never silently trust-all). |
| 7 | Remote Docker reachability lesson honored (no 2375 prerequisite) | 11.2, 24.1 | C, K | Partial | Phase C's architecture already satisfies this: LOCAL mode is the default (no TCP exposure needed), REMOTE is explicit opt-in never inferred/required, and no diagnostic message ever advises exposing port 2375 (DockerDiagnosticsTest#noClassifiedMessageEverAdvisesExposingPort2375). Phase K still owes the portable-Compose packaging that demonstrates this in practice. |
| 8 | Java 21 / Spring Boot 3.x / WebFlux | 4.1 | A, B | Done | backend/pom.xml (Spring Boot 3.5.16 parent, Java 21); backend/src/main/java/com/logexplorer/LogExplorerApplication.java; spring-boot-starter-webflux dependency; real `./mvnw -q verify` run (83/83 tests) and a real `java -jar` smoke boot (docs/verification/PHASE_B_REPORT.md). |
| 9 | React / TypeScript / Vite, strict TS | 4.2 | A, F | Missing | Absent — repo contains no application code (backend/ and frontend/ do not exist). docs/AUDIT.md §5 row 9. |
| 10 | No MVP database; safe localStorage only | 4.4 | B, F | Done | Backend: no database dependency of any kind in backend/pom.xml. Frontend: `localStorage`/`sessionStorage`/URL are never written at all this phase (verified with real spies across a full interaction sequence, `frontend/src/app/persistence.test.tsx`). |
| 11 | Single deployable image, SPA fallback excludes /api and actuator | 4.3 | K | Done | Multi-stage `Dockerfile` (Vite build -> Maven build embedding it as Spring Boot static resources -> non-root JRE runtime); `SpaWebFluxConfig.java` serves it with a `PathResourceResolver` fallback that explicitly excludes `api/` and `actuator` from ever falling back to `index.html` - found and fixed a real bug in this exact fallback during its own first test run (an unmapped `/api/...` path was being silently served the app shell instead of a 404). `SpaFallbackIntegrationTest` (5 tests); confirmed against the real built image (`curl` against a running container: an unknown client route returns the real `index.html`, a real JS asset serves correctly, a bad `/api/...` path returns a clean 404, `/actuator/health` and `/api/v1/sources` both return real JSON). |
| 12 | Canonical top-level fields mapped | 5, 5.1 | B | Done | backend/src/main/java/com/logexplorer/core/parse/LogLineParser.java; LogLineParserTest#mapsAllCanonicalTopLevelFields (23 parser tests total, all passing). |
| 13 | All expected MDC fields handled | 5 | B | Done | LogLineParser.java mdc extraction; LogLineParserTest#mapsAllCanonicalMdcFields. |
| 14 | Literal dotted key `event.correlationId` read safely | 5.1 | B | Done | LogLineParser#resolveCorrelationId uses Map.get("event.correlationId") literally; LogLineParserTest#correlationLiteralKeyIsNotMistakenForANestedPath explicitly proves a nested {event:{correlationId}} shape is NOT picked up. |
| 15 | Correlation precedence (X-Correlation-id → event.correlationId) | 5.1 | B | Done | LogLineParser#resolveCorrelationId; LogLineParserTest#correlationPrefersHeaderKeyWhenBothPresent, #correlationFallsBackToLiteralDottedKeyWhenHeaderAbsent. |
| 16 | Unknown JSON/MDC fields preserved | 5.3 | B | Done | CanonicalLogEvent.unknownTopLevelFields/unknownMdcFields; LogLineParserTest#preservesUnknownTopLevelFields, #preservesUnknownMdcFields, #unknownMdcFieldsNeverContainAnyOfTheFiveSensitiveKeys. |
| 17 | Malformed lines become raw fallback events | 5.4 | B | Done | LogLineParser#malformed(); LogLineParserTest#malformedNonJsonLineBecomesRawFallbackEvent, #jsonArrayInsteadOfObjectBecomesRawFallbackEvent, #nullLineBecomesRawFallbackEventWithoutThrowing. |
| 18 | Timestamp normalization to Instant, no double conversion | 5.5 | B | Done | LogLineParser#parseTimestamp (OffsetDateTime/Instant parsing, keeps timestampRaw always); LogLineParserTest#acceptsVariousTimestampOffsetsAndNormalizesToUtcInstant (parameterized, 4 offset shapes). |
| 19 | Sensitive field masking before browser serialization | 6.1 | B | Done | core/mask/MaskingService.java (single boundary, applied in api/EventMapper.java before DTO construction); MaskingServiceTest (19 tests incl. boundary cases); SerializationLeakTest proves masking is applied before serialization. |
| 20 | Never log search values, tokens, raw identifiers, events | 6.2 | B + every phase | Partial | Phase B: SerializationLeakTest + LogLeakTest (real Logback capture across success/guardrail-violation/unknown-source paths, response body AND logs both checked, real WebClient/Netty TRACE wire-logging leak found and fixed via logging.level pins in application.yml). Ongoing responsibility for every later phase per HANDOVER.md §6.2 - not yet 'Done' project-wide. |
| 21 | Safe text rendering; no dangerouslySetInnerHTML | 6.3 | G, H | Done | Phase G's own share (results table). Phase H: `frontend/src/test/noDangerousHtml.test.ts` scans every `.ts`/`.tsx` file under `src/` via `import.meta.glob` for real `dangerouslySetInnerHTML=` usage (substitutes for the "lint rule" the plan calls for - this repo has no ESLint configured at all, a Phase A-audited baseline; see PHASE_H_REPORT.md) - zero found. The inspector's exception block and raw-JSON disclosure both render via `{...}` text interpolation into a `<pre>`, never HTML injection. |
| 22 | Explicit source capability model | 7 | B | Done | core/model/SourceCapabilities.java; returned via GET /api/v1/sources (SourcesController.java); SourcesApiIntegrationTest#sourcesEndpointReturnsExplicitCapabilitiesJson. |
| 23 | Source registry and API endpoints | 7 | B | Done | source/LogSource.java (SPI), source/LogSourceRegistry.java (stable-ID resolution, unknown/disabled -> sanitized ProblemDetail); LogSourceRegistryTest (5 tests); SourcesApiIntegrationTest (404 cases). |
| 24 | Bounded query guardrails | 8.1 | B | Done | core/guard/SearchGuardrails.java; SearchGuardrailsTest (12 tests: missing/invalid/oversized range, limit validation and clamping). |
| 25 | Per-source maximum time range | 8.1 | B | Done | SearchGuardrailsProperties#perSourceMaxTimeRange (Map<String,Duration>, config-driven, no hardcoding); SearchGuardrailsTest#perSourceMaxTimeRangeOverridesTheDefaultAndIsEnforced, #perSourceMaxTimeRangeCanBeMorePermissiveThanTheDefault. |
| 26 | Result limit with configurable max (≤ 5,000) | 8.1 | B | Done | SearchGuardrailsProperties#maxLimit (default 5000, matching HANDOVER.md §8.1's "<=5000"); SearchGuardrailsTest#limitAboveConfiguredMaxIsClampedNotRejected. |
| 27 | Bounded concurrency | 8.1 | B | Done | core/guard/ConcurrencyGuard.java (non-blocking Semaphore, releases on complete/error/cancel); ConcurrencyGuardTest (3 tests); SearchServiceTest#concurrencyCapIsEnforcedAcrossSearchesOnTheSameSource. |
| 28 | Cancellation propagated | 8.1, 18.2 | B, J | Done | Phase B (search cancellation): ConcurrencyGuard#guard releases on doFinally incl. cancel; SearchServiceTest#cancellationPropagatesDownToTheUnderlyingSource proves cancellation reaches the LogSource. Phase J (live-tail cancellation): `DockerLogSource#follow` wires `sink.onDispose(() -> closeAll(callbacks))`, closing the real `Closeable` docker-java hands back via `ResultCallback.Adapter#onStart` - proven with a captured mock in `DockerLogSourceTest#cancellationClosesTheUnderlyingDockerFollowCallback`; frontend closes the real `EventSource` on Stop/Exit/unmount (`useLiveTail.ts`, `useLiveTail.test.ts`). |
| 29 | Simple deterministic query DSL (no eval/SpEL/reflection) | 9 | E | Done | core/query/** (QueryLexer, QueryParser, ast.*, QueryEvaluator, QueryPlanExplainer) - closed AST algebra, direct field comparisons only, no eval/SpEL/reflection anywhere (grep-verified). QueryParserTest (30), QueryEvaluatorTest (11), QueryPlanExplainerTest (5), QueryRedactionTest (6). Real end-to-end verification via a live backend against both the fixture (predicate) and mock-Loki (planner) paths (docs/verification/PHASE_E_REPORT.md). |
| 30 | Raw LogQL gated: Loki-only, off by default, config-enabled, bounded | 9.1 | E | Done | SearchRequest#rawLogQl gated centrally in SearchService via source.capabilities().rawLogQL() (Loki-only + config-enabled in one check); LokiLogSource uses it verbatim only when LokiProperties#rawLogQlEnabled, else defensively rejects. Still passes through the same time/limit/timeout/concurrency guardrails (SearchServiceTest, LokiLogSourceTest, QueryApiIntegrationTest). |
| 31 | Docker Compose label discovery + service counts | 10 | C | Done | ComposeLabels.java (com.docker.compose.project/service label access) + DockerLogSource#discoverServicesBlocking. ComposeLabelsTest + DockerLogSourceTest#discoverServicesOnlyIncludesComposeManagedContainersAndCountsRunningVsTotal + real discovery against live labeled containers (docs/verification/PHASE_C_REPORT.md), incl. stopped-but-readable containers. |
| 32 | Docker stream framing decoded correctly (incl. tty) | 10 | C | Done | DockerFrameCollectingCallback.java demuxes via Frame.getStreamType() (STDOUT/STDERR/RAW for Tty=true containers, treated as stdout since Docker doesn't multiplex them). DockerFrameCollectingCallbackTest (8 tests: split-across-frames, interleaved stdout/stderr, RAW/tty, truncation, trailing partial line). |
| 33 | Docker operations strictly read-only | 10 | C | Done | DockerLogSource depends only on ReadOnlyDockerClient, a facade exposing exactly 5 read-only operations - mutating DockerClient methods are structurally unreachable, not just unused. ReadOnlyDockerClientMethodSetTest locks the facade's method set. |
| 34 | OpenShift Loki gateway adapter, query_range semantics | 12 | D | Done | source/loki/LokiLogSource.java + LokiQueryClient.java (query_range, nanosecond start/end, direction, limit). LokiQueryClientTest (12 tests) + LokiLogSourceTest (13 tests) + real end-to-end verification against a live backend + tools/mock-loki (docs/verification/PHASE_D_REPORT.md). |
| 35 | Gateway prefix / tenant / namespace + service label keys configurable | 12 | D | Done | config/LokiProperties.java - gatewayPrefix/tenant/namespaceLabelKey/serviceLabelKey/podLabelKey/containerLabelKey all externally configurable, nothing hardcoded. LokiQueryClientTest#queryConstructionVariesCorrectlyWithADifferentGatewayPrefixAndTenant proves a genuinely different route shape resolves. |
| 36 | TLS verification enabled for Loki; no trust-all | 6.5, 12 | D | Done | source/loki/LokiWebClientFactory.java + CompositeX509TrustManager.java - real TLS handshake tests (LokiWebClientFactoryTest, 3 tests) against a self-signed cert: rejected with no caCertPath, accepted once added as an extra trusted CA, still rejected with an unrelated CA configured (proves no accidental trust-all). |
| 37 | Compact professional shell, one title, health + retry | 13.1 | F | Done | frontend/src/app/Shell.tsx (single "Log Explorer" title, active source name, SourceHealthBadge with retry) + Toolbar.tsx (full default control order). Toolbar.test.tsx proves exact order; SourceHealthBadge.test.tsx proves retry. Real end-to-end verification against a live backend (docs/verification/PHASE_F_REPORT.md). |
| 38 | Accessible searchable service multi-select | 13.3 | F | Done | frontend/src/features/search/ServiceMultiSelect.tsx - checkbox list (not native `<select multiple>`), search-box filter, running/total metadata, Clear, Escape/outside-click dismiss. ServiceMultiSelect.test.tsx (9 tests incl. jest-axe). |
| 39 | Severity default INFO/WARN/ERROR; not color-only | 13.4 | F | Done | frontend/src/features/search/SeverityFilter.tsx + severityLevels.ts - every chip always shows its text label plus a color dot, `aria-pressed` conveys active state. SeverityFilter.test.tsx. |
| 40 | Universal search with confirmable ID detection | 13.5 | F | Done | frontend/src/features/search/UniversalSearch.tsx + idDetection.ts - exact required label, Enter/Ctrl+Enter run, Escape dismisses the suggestion without clearing typed text, confirming never happens silently. UniversalSearch.test.tsx (10 tests) + idDetection.test.ts (7 tests). |
| 41 | Advanced filters grouped by question, draft/apply/cancel, protected wording | 13.6 | F | Done | frontend/src/features/search/AdvancedFilters.tsx + advancedFilterFields.ts (4 groups, exact field lists) + ActiveFilters.tsx ("Protected" for the 5 sensitive fields, never the raw value). AdvancedFilters.test.tsx (9 tests) + ActiveFilters.test.tsx (5 tests) prove editing never calls onApply and Cancel discards the draft. |
| 42 | Time presets include Last 1 day | 14.1 | F | Done | shared/time/presets.ts (`DEFAULT_PRESET_ID = '1d'`, label "Last 1 day"). TimeRangeControl.test.tsx. |
| 43 | Zero-result one-click "Search last 1 day" | 14.1 | F, G | Done | frontend/src/features/results/ResultsPanel.tsx - the real results area's empty state offers the one-click "Search last 1 day" button, using the same DEFAULT_PRESET_ID. ResultsPanel.test.tsx. |
| 44 | Custom range popover: prefill, Apply/Cancel, close, focus restore, real interval label | 14.2 | F | Done | features/timerange/{TimeRangeControl,CustomRangePopover,prefill}.tsx - temporary popover (never permanently expanded), exact prefill rule (reopen restores committed; else End=now/Start=end−previousPresetDuration/30min fallback), Apply validates and commits, Cancel/Escape/outside-click discard, focus restored to the trigger (usePopoverTrigger), real formatted interval shown (never "Custom range"). TimeRangeControl.test.tsx (12 tests) + prefill.test.ts (3) + label.test.ts (2) + interval.test.ts (12). Real browser screenshots (docs/verification/f/). |
| 45 | Display zone shown (e.g. Asia/Kuwait UTC+03:00) | 14.2 | F | Done | shared/time/timezone.ts `formatZoneLabel` - real browser verification in this environment (Asia/Kuwait) produced the exact example format from the plan. timezone.test.ts. |
| 46 | Display zone → UTC converted exactly once | 14.2, 24.9 | F | Done | shared/time/interval.ts `utcIsoFromLocalDateTimeInput` is the single conversion point, called only at Apply; committed state is always UTC ISO thereafter. interval.test.ts's round-trip test proves no drift. |
| 47 | Time validation distinguishes all four error classes | 14.2 | F | Done | shared/time/interval.ts `validateRange` - missing-start, missing-end, start-not-before-end, future-end, max-range-exceeded, each with its own reason and message. interval.test.ts covers all five (including the boundary case exactly at the max). |
| 48 | Exactly seven columns in exact order | 15.1 | G | Done | frontend/src/features/results/columns.ts (RESULT_COLUMNS, single source of truth for both colgroup and thead) + ResultsTable.tsx. ResultsTable.test.tsx; real browser verification against live fixture data (docs/verification/PHASE_G_REPORT.md). |
| 49 | Missing values render `—`; no omitted cells | 15.1, 24.5 | G | Done | shared/ui/table/emptyValue.ts (EMPTY_VALUE, one shared constant) used by every column renderer in columnMapping.ts; every `<td>` always renders, never conditionally omitted. ResultsTable.test.tsx's "never omits a cell" test. |
| 50 | One semantic table, one colgroup, fixed layout, shared geometry | 15.2, 24.4 | G | Done | ResultsTable.tsx - one `<table>`, one `<colgroup>`, `table-layout: fixed`; header and body are literally the same table, no independent grid/flex system. Real browser geometry assertions (≤2px header/cell left+width match) at all 6 required viewports + 125%/200% zoom, all passing (`e2e/phase-g-results-table.spec.ts`). |
| 51 | Actions is the seventh cell of the same row | 15.3, 24.7 | G | Done | ActionsCell.tsx rendered as the 7th `<td>` in the same `<tr>`; ResultsTable.test.tsx proves every row has exactly 7 `<td>` cells, no second action row. |
| 52 | Message renders under "What happened", not Service | 15.1, 24.6 | G | Done | columnMapping.ts `resolveWhatHappened`/`resolveService` are independent functions reading independent fields; ResultsTable.test.tsx explicitly asserts the message text never appears in the Service cell. |
| 53 | Newest-first sort; no dropped or duplicated rows | 15.6 | G | Done | ResultsTable renders events in exactly the order the backend returns (no client reordering) - the backend's own newest-first sort is already proven per-source in Phases B-E; ResultsTable.test.tsx proves the frontend never reorders; real browser test asserts real fixture timestamps are non-increasing row over row. |
| 54 | One pagination model only | 15.7 | G | Done | ResultsPanel.tsx renders exactly one "Load more" control, gated strictly on a real `nextCursor` (never a competing Prev/Next). Currently unreachable in practice - no adapter populates `nextCursor` yet (`SearchService#toResult` always returns null) - built and tested correctly regardless (`useSearchState.test.ts`, `ResultsPanel.test.tsx`), so it activates the moment a future phase wires real server-side cursor pagination. |
| 55 | Truthful, non-contradictory counts | 15.8, 24.10 | G | Done | features/results/counts.ts `buildCountsSummary` keeps estimatedTotal/returned/visible/truncated distinct, never conflated; counts.test.ts (6 tests) incl. the visible-vs-returned-fetched distinction. |
| 56 | ≤2px header/cell geometry verification at all viewports and zoom | 15.9 | G | Done | e2e/phase-g-results-table.spec.ts - real browser, real fixture data, `assertTableGeometry` (≤2px tolerance) at 1920/1440/1280/1024/768/390 and 125%/200% zoom, all passing, screenshots committed (docs/verification/g/). |
| 57 | Loading state | 27.6 | G | Done | ResultsPanel.tsx shows a "Searching…" `role="status"` state while `searchLoading`. ResultsPanel.test.tsx. |
| 58 | Error state | 27.6 | G | Done | ResultsPanel.tsx shows a `role="alert"` error state, mutually exclusive with results. ResultsPanel.test.tsx proves error and table are never shown together. |
| 59 | Empty state | 27.6 | G | Done | ResultsPanel.tsx's empty state (see row 43). |
| 60 | Cancelled state | 27.6 | G | Partial | Implemented as request-supersession protection: `useSearchState` aborts any still-in-flight search the moment a new one starts, so a stale response can never overwrite a fresher one (real `AbortController` wiring, proven with a real out-of-order-resolution test in `useSearchState.test.ts`). Not a separate visible UI state, since there is no Cancel button anywhere in this UI to trigger a user-facing cancellation from - documented as a deliberate scope boundary in docs/verification/PHASE_G_REPORT.md. |
| 61 | Partial / truncated state | 8.1, 27.6 | G | Done | `counts.truncated` flows into `buildCountsSummary`'s copy honestly (row 55); malformed-line rendering (also part of HANDOVER §27.6's state list) shown via MessageCell's "malformed" badge + raw line text. |
| 62 | Event inspector layout, resize, prev/next/close, focus | 16 | H | Done | `features/inspector/EventInspector.tsx` - side panel beside results ≥1024px, dismissable full-height sheet ≤1024px (`EventInspector.module.css` media query), `useResizablePanel.ts` clamps [320,720]px with keyboard (Arrow keys on the `role="separator"`) and pointer drag, Previous/Next/Close in `InspectorHeader.tsx`, focus moves to Close on open and restores to the opener on close (`useSearchState.ts` `openInspector`/`closeInspector` + `useSearchState.focusRestoration.test.tsx`). Real-browser: `e2e/phase-h-event-inspector.spec.ts` (16 tests: sections render, prev/next bounds, Escape/backdrop/Close all dismiss + restore focus, side-panel-vs-overlay at 1920/1440/1280/1024/768/390, no page overflow incl. 125%/200% zoom, resize bounds). |
| 63 | Local timestamp with ms + named zone, plus UTC | 16.2 | H | Done | `features/inspector/timestampFormat.ts` (`formatLocalTimestamp`/`formatUtcTimestamp`/`localZoneLabel`) + `OverviewSection.tsx`; unit tests in `timestampFormat.test.ts`. |
| 64 | Protected actor & client section, no reveal action | 16.3 | H | Done | `features/inspector/ActorClientSection.tsx` - every field sourced from `event.protectedFields` (already-masked, the only sensitive carrier `LogEvent` has), labelled "Protected / masked", no reveal control anywhere; "when present" fields omitted individually, honest empty-state note when none present. `ActorClientSection.test.tsx` asserts masked values render and no reveal-shaped button exists; real fixture-data check in `phase-h-event-inspector.spec.ts`. |
| 65 | Request-flow section with safe copy and related-log actions | 16.4 | H | Done | `features/inspector/RequestFlowSection.tsx` - journey/correlation/trace/span/event IDs only (never a sensitive field), each with Copy (`shared/browser/clipboard.ts`) and "Find related logs" (`useSearchState.ts#findRelated` - re-runs the current source's search with every other filter cleared and only that ID set). `RequestFlowSection.test.tsx` asserts no sensitive-field action ever renders; real-browser action in `phase-h-event-inspector.spec.ts`. |
| 66 | Business/error section with formatted exception | 16.5 | H | Done | `features/inspector/BusinessErrorSection.tsx` - business step/UI identifier/error code plus the exception in a `<pre>` (text interpolation only, `white-space: pre-wrap`) preserving multiline stack traces as one legible block. `BusinessErrorSection.tsx`'s own doc comment plus `AllFieldsSection`'s no-dangerouslySetInnerHTML test cover the "text rendering only" requirement. |
| 67 | All-fields view with raw JSON behind disclosure | 16.6 | H | Done | `features/inspector/AllFieldsSection.tsx` + `allFields.ts` - canonical fields first (raw-presence filtered, never a wall of `—` rows), unknown top-level/MDC fields after (never discarded), a search box filtering both, raw JSON behind a collapsed `<details>` (`JSON.stringify(event, null, 2)` as one `<pre>` text node). `allFields.test.ts` + `AllFieldsSection.test.tsx` (incl. an explicit "single text node, never dangerouslySetInnerHTML" assertion). |
| 68 | Show ±30 seconds context, scoped, bounded, with breadcrumb | 16.7 | H | Done | New `POST /api/v1/logs/context` endpoint (`SearchController#context`, `RequestMapper#toContextDomain`) - a fixed ±30s window computed server-side from the event's own timestamp (never client-widened), scoped to service/container/pod when present (`EventFilters` extended). Frontend: `features/inspector/ContextAction.tsx` previews the exact bounded window before running (Run/Cancel confirm step), `useSearchState.ts#showContext` calls the dedicated endpoint and sets a "back to original search" breadcrumb (`ResultsPanel.tsx`'s `Breadcrumb`). Backend: `RequestMapperTest`, `ContextApiIntegrationTest` (real HTTP), `EventFiltersTest` (containerId/pod). Frontend: `useSearchState.test.ts` (dedicated-endpoint + window-size assertions), `ContextAction.test.tsx` (preview-before-execution). Real-browser: `phase-h-event-inspector.spec.ts`. |
| 69 | Trace investigation | 17 | I | Done | "Find this Trace ID" - `RequestFlowSection.tsx` (inspector) and the Correlation/Trace column's own clickable ID (`ResultsTable.tsx`) - both call `useSearchState.ts#openJourney('traceId', value)`, hitting the new `POST /api/v1/logs/journey` endpoint. Real fixture data: `phase-i-journey-investigation.spec.ts`. |
| 70 | Correlation investigation | 17 | I | Done | Same mechanism as row 69, `field: 'correlationId'` - the Correlation/Trace column falls back to showing/linking the correlation ID when no trace ID is present (`columnMapping.ts#resolveCorrelationOrTrace`). |
| 71 | Journey investigation across multiple traces | 17 | I | Done | "Find this Journey ID" opens `features/journey/JourneyView.tsx`, which never filters to one trace - a journeyId match legitimately spans many. `journeyFields.ts#countDistinctTraces` counts and the summary line states it explicitly. Real fixture data confirms (one real journey: 6 events, 6 distinct traces, 3 services - see PHASE_I_REPORT.md). `journeyFields.test.ts`, `JourneyView.test.tsx`. |
| 72 | JMS / event correlation metadata displayed | 17 | I | Done | Each `JourneyEntryRow.tsx` shows Trace/Span/Correlation/Event IDs "when available" - `correlationId` is exactly the JMS-style correlation convention CLAUDE.md §4's parsing rules already document (`mdc["event.correlationId"]`). `JourneyEntryRow.test.tsx`. |
| 73 | Timestamp order stated as not guaranteed causality | 17 | I | Done | A persistent, never-omitted disclaimer (`role="note"`) rendered whenever the timeline has any entries: "Ordered by timestamp — this does not indicate causality between events." `JourneyView.test.tsx` asserts it's present; real-browser check in `phase-i-journey-investigation.spec.ts`. |
| 74 | Previous search state preserved and restorable | 17 | I | Done | Journey mode is a pure overlay (`useSearchState.ts#openJourney`/`closeJourney` never touch `searchResult` or any toolbar filter) - "restoring" is by construction, not a snapshot/restore mechanism like Phase H's Context/Find-related. `useSearchState.test.ts` proves `searchResult`/`timeRange` are byte-identical before and after a journey lookup; real-browser "Back to search results" check in `phase-i-journey-investigation.spec.ts`. |
| 75 | SSE live transport; no tokens or sensitive filters in URL | 18.1 | J | Done | `LiveTailController#live` (`GET /api/v1/logs/live`, `produces=TEXT_EVENT_STREAM_VALUE`); the query string carries only `sourceId`/`services` - never a search value or token (`useLiveTail.ts#start` builds the URL from exactly those two, `LiveTailServiceTest`/`LiveTailControllerTest`, real-browser `phase-j-live-tail.spec.ts`, and a real `curl` against the running dev backend confirming masked `data:` payloads). |
| 76 | Docker upstream cancellation on disconnect | 18.2 | J | Done | `DockerLogSource#follow` + `DockerFollowCallback` - `sink.onDispose(() -> closeAll(callbacks))` closes the real docker-java `Closeable` when the Reactor subscription is disposed. `DockerLogSourceTest#cancellationClosesTheUnderlyingDockerFollowCallback` (captured `Closeable` mock, `verify(..., timeout(2000)).close()`). |
| 77 | Loki live capability gated honestly; no faking | 18.3, 24.14 | D, J | Done | Phase D: `LokiProperties#liveTailSupported` (default false, config-gated); `LokiLogSource#capabilities().liveTail()` reflects it honestly. Phase J: `LiveTailService#follow` throws `GuardrailViolationException(LIVE_TAIL_NOT_SUPPORTED)` (real HTTP 400) for any source whose capability is false - `LiveTailServiceTest#aSourceThatDoesNotSupportLiveTailIsRejectedOutright`, `LiveTailControllerTest#aSourceThatDoesNotSupportLiveTailReturns400WithTheHonestReason`; frontend never renders the Live button for such a source (`Toolbar.tsx`, `Toolbar.test.tsx`), confirmed against the real `openshift-loki` source in `phase-j-live-tail.spec.ts`. |
| 78 | Bounded backend buffer, heartbeat, timeout, max concurrent tails | 18.4 | J | Done | `LiveTailService#follow` - `onBackpressureBuffer(serverBufferSize, ..., DROP_OLDEST)` with an honest drop count reported via a periodic `status` SSE event, `.take(connectionTimeout)`, `LiveTailGuard` (semaphore) capping concurrent tails at `maxConcurrentTails` - all sourced from `logexplorer.live.*` (`application.yml`), nothing hardcoded. A real bug was found and fixed this phase: the heartbeat `Flux.interval` crashed the *entire* connection under genuine slow-consumer backpressure without `.onBackpressureLatest()` - see PHASE_J_REPORT.md. `LiveTailServiceTest` (7 tests incl. the regression test and the concurrent-cap-is-wired test), `LiveTailGuardTest`. |
| 79 | Bounded frontend buffer | 18.4 | J | Done | `useLiveTail.ts` - the visible list is capped at `VISIBLE_CAP` (evicting oldest, counted in `clientDroppedCount`); the separate paused-events buffer is capped identically so a long pause cannot grow memory unboundedly either. `useLiveTail.test.ts` (16 tests). |
| 80 | Initial displayed-live cap of 1,000 events | 18.4 | J | Done | `liveTailTypes.ts#VISIBLE_CAP = 1000`; `useLiveTail.test.ts#exceeding the 1,000-event visible cap evicts the oldest and counts it as a client drop`. |
| 81 | Start / pause / resume / stop lifecycle, unmount closes stream | 18.4 | J | Done | `useLiveTail.ts` (start/pause/resume/stop/exit) + `LiveTailPanel.tsx` (state-gated controls); unmount closes the real `EventSource` (`useEffect(() => closeEventSource, ...)`). `useLiveTail.test.ts` (incl. an explicit unmount-closes-stream test), `LiveTailPanel.test.tsx` (15 tests, per-state button visibility); real-browser lifecycle in `phase-j-live-tail.spec.ts` (Start via the toolbar's Live button, Pause/Resume, Stop-then-restart, Back-to-search-results-then-fresh-session). |
| 82 | Dropped and buffered counts visible | 18.4 | J | Done | `LiveTailPanel.tsx` shows Received / Visible / Buffered-while-paused / Dropped(display cap) / Dropped(server buffer full) as distinct, never-conflated values. `LiveTailPanel.test.tsx#renders received/visible/dropped/buffered counts distinctly`; real-browser confirmation that "Buffered while paused" appears during an actual pause in `phase-j-live-tail.spec.ts`. |
| 83 | Responsive at 1920/1440/1280/1024/768/390 | 19.2, 28 | F–H, M | Missing | Absent — repo contains no application code (backend/ and frontend/ do not exist). docs/AUDIT.md §5 row 83. |
| 84 | Accessibility to WCAG 2.2 AA principles | 19.4 | F–J | Partial | Phase F's own share: every control has a labeled accessible name, keyboard-operable (native buttons/inputs/checkboxes, Escape/outside-click to dismiss), visible `:focus-visible` ring, `VisuallyHidden` screen-reader labels, non-color severity meaning, logical focus restoration on popover close (usePopoverTrigger), `prefers-reduced-motion` respected. jest-axe run against every component (0 violations) - see each component's own test file. Phase J's own share: `LiveTailPanel.tsx` - state changes announced via `role="status"`, errors via `role="alert"`, every control a labeled native button; jest-axe 0 violations (`LiveTailPanel.test.tsx`). |
| 85 | Zoom/reflow at 125% / 200% and targeted high zoom | 28 | F, G | Done | Phase F: time-range popover at 125%/200%/400% zoom. Phase G: results table geometry at 125%/200% zoom (the plan's own explicit bar for this phase - 400% is Phase F's "targeted high zoom for the time-control overlap case" specifically, not repeated here). All real browser tests, all passing. |
| 86 | No page-level horizontal overflow | 15.2, 19.4 | G | Done | The results table scrolls internally (`.scrollWrapper { overflow-x: auto }`) rather than the page - real browser verification at 200% zoom shows exactly this (docs/verification/g/results-table-zoom-200pct.png). Also found and fixed a real cross-phase bug this phase's real-data verification exposed: the Shell header (Phase F) had no `flex-wrap`, so a genuinely unhealthy source's wider "Retry" content could overflow the page at high zoom - fixed alongside this phase's own work (see docs/verification/PHASE_G_REPORT.md). |
| 87 | Centralized design tokens; restrained visual language | 19.3 | F | Done | frontend/src/shared/tokens.css - color/typography/spacing/radius/border/shadow/motion as CSS custom properties, one restrained accent, semantic severity colors (never color alone). Every component styled via CSS Modules referencing these tokens, no inline uncontrolled colors. |
| 88 | No fabricated branding or production-readiness claims | 3 | F, M | Present | README.md:1-2 is neutral ('Log Explorer...'); no branding/logo/affiliation/production claims found anywhere in repo. docs/AUDIT.md §5 row 88. |
| 89 | Non-root runtime image, multi-stage, no build secrets | 4.3, 21 | K | Done | Three-stage `Dockerfile` (frontend build / backend build / runtime); runtime never declares `USER logexplorer` directly - `docker-entrypoint.sh` starts as root only to align a supplementary group with whatever GID the host's mounted docker.sock happens to have, then drops to the non-root `logexplorer` user via `su-exec` *before* ever exec'ing the JVM. Verified directly on a real running container, every profile: `docker exec ... cat /proc/1/status` shows the java process's real GID/supplementary groups, never root, both with and without the socket mounted. `docker history --no-trunc` scanned for secret-shaped strings - none found. |
| 90 | Docker socket mount only behind explicit Compose profile | 20.1 | K | Done | `docker-compose.docker-socket.yml` - a *second*, separately-`-f`'d override file, itself still profile-gated to `docker-socket` on top of that (two independent explicit actions required, never one flag away from an accident) - adds the read-only socket mount to `app`; the base `docker-compose.yml` never mounts it under any profile. Verified: `docker inspect` on the running container confirms `"Mode":"ro","RW":false` only when both the override file and the profile are explicitly passed. |
| 91 | Docker socket privilege warning documented | 20.1 | K | Done | Prominent warning in both `docker-compose.docker-socket.yml`'s own header comment and `docs/RUN_GUIDE.md`'s dedicated "Docker socket privilege warning" section - explains the Engine API is not read-only at the daemon level regardless of this codebase's own read-only client, and that `:ro` restricts the socket node's filesystem operations, not the API's own read surface. |
| 92 | `.env.example` with names and harmless defaults only | 6.4 | K | Done | `.env.example` - every `logexplorer.*` configuration property from all 5 `@ConfigurationProperties` classes (incl. Phase J's `live.*`, added after this file's own Phase C/D-era first draft) plus the Compose-level knobs (`APP_PORT`, `MOCK_LOKI_PORT`, `DEMO_LOG_GENERATOR_SEED`, `SPRING_PROFILES_ACTIVE`) - names and harmless/default values only, no secret; the Loki bearer token is represented only as `LOGEXPLORER_LOKI_TOKEN_ENV_VAR`, the *name* of another variable holding it, never the token itself. A real bug in this same area was found and fixed this phase: a blank (declared-but-empty) value for `LOGEXPLORER_DOCKER_COMPOSE_PROJECT_FILTER`, passed through Compose's `env_file` mechanism, was silently filtering out every real container - see row 3's evidence and `DockerLogSourceTest#aBlankComposeProjectFilterIsTreatedAsNoFilterNotAsAnEmptyProjectName`. |
| 93 | Portable run/requirements guide matching real commands | 20 | K | Done | `docs/RUN_GUIDE.md` - requirements, quick start, all three Compose profiles and their combinations, ports, env vars, the Docker socket privilege warning, remote Docker options (default/custom port, optional TLS), real and mock OpenShift Loki configuration, troubleshooting. Every command in it was actually run this phase against this real repository, not written from assumption - including the exact fresh-clone-equivalent sequence `scripts/smoke.sh` automates. Explicitly states remote Docker TCP exposure is never required. |
| 94 | Deterministic smoke tests (build→start→health→discover→search→UI→stop→cleanup) | 20 | K | Done | `scripts/smoke.sh` - build, start, health-poll, source discovery, search (asserts real fixture events, not just a 200), UI shell load, and an added SPA-fallback-scope check, then stop + cleanup **limited to this stack only** (a shell `trap` guarantees cleanup runs even on failure; never a global prune). Run for real this phase, from a cold `.env`-less state (exercising the documented "no `.env` yet" auto-copy path too): **SMOKE TEST PASSED**, clean teardown confirmed (`docker ps -a` empty afterward). |
| 95 | OpenShift manifests (Deployment, Service, Route, ConfigMap, ServiceAccount) | 21 | L | Missing | Absent — repo contains no application code (backend/ and frontend/ do not exist). docs/AUDIT.md §5 row 95. |
| 96 | No cluster-wide RBAC; namespace-scoped binding documented only | 21 | L | Missing | Absent — repo contains no application code (backend/ and frontend/ do not exist). docs/AUDIT.md §5 row 96. |
| 97 | Secret references only; no committed secrets | 21 | L | Partial | No committed secret values found in full git history scan (docs/AUDIT.md §6); but no deploy/ manifests exist yet to apply the 'Secret references only' pattern to. docs/AUDIT.md §5 row 97. |
| 98 | Independent verification per phase, not compilation | 22 | all, M | Present | This audit reports real command output incl. failures (docs/AUDIT.md §3) rather than compilation-as-proof. docs/AUDIT.md §5 row 98. |
| 99 | Recovery process after verification failure | 22.2 | all | Present | Recovery process defined in PHASE-PROMPTS.md and IMPLEMENTATION-PLAN.md §7 per-phase Recovery sections. docs/AUDIT.md §5 row 99. |
| 100 | PASS / FAIL / BLOCKED / DEFERRED honesty | 22.1, 24.15 | all | Present | Applied throughout docs/AUDIT.md (FAIL/BLOCKED reported honestly, not converted to PASS). docs/AUDIT.md §5 row 100. |
| 101 | Final stakeholder acceptance tasks 1–6 | 27 | M | Missing | Absent — repo contains no application code (backend/ and frontend/ do not exist). docs/AUDIT.md §5 row 101. |
| 102 | Deferred items and non-goals preserved, not implemented | 25 | M | Present | No out-of-scope capability found anywhere in repo (trivially true — no code exists at all). docs/AUDIT.md §5 row 102. |

---

## Superseded decisions — confirm each was applied, not reverted

| Decision | Applied? | Evidence |
|---|---|---|
| Remote Docker TCP is optional, never a prerequisite | N/A yet | No Docker adapter code exists to apply this to (docs/AUDIT.md §5 rows 4-7). Owning phase C. |
| Remote Docker TLS optional (but never trust-all) | N/A yet | No Docker adapter code exists (docs/AUDIT.md §5 row 6). Owning phase C. |
| Remote Docker default port prefilled and overridable | N/A yet | No Docker adapter code exists (docs/AUDIT.md §5 rows 4-5). Owning phase C. |
| Portable Compose is a first-class deployment mode | Applied | `docker-compose.yml` + `docker-compose.docker-socket.yml`, all three profiles (`demo`/`docker-socket`/`loki-mock`) built and run for real this phase, incl. real container discovery through the `docker-socket` profile end to end. See row 3 and `docs/verification/PHASE_K_REPORT.md`. |
| Live OpenShift verification is DEFERRED, not a release blocker | Applied | Loki adapter built and mock-verified (docs/verification/PHASE_D_REPORT.md); no real OpenShift cluster reachable from this environment, so live-cluster verification is recorded as **DEFERRED**, explicitly not converted to PASS and not treated as a release blocker, per IMPLEMENTATION_PLAN.md "Phase D"'s own allowance. |
| Raw LogQL is off by default and not a dominant disabled control | Partial | Backend half applied: LokiProperties#rawLogQlEnabled defaults to false, config-enabled only (Phase E). The UI half ("not a dominant disabled control") has no UI to apply it to yet - owning phase F. |
| One deployable image with SPA fallback excluding /api and actuator | Applied | Multi-stage `Dockerfile` + `SpaWebFluxConfig.java`. A real bug in the fallback's own first implementation (a bad `/api/...` path silently served the app shell) was found and fixed this phase - see row 11. |
| Committed custom time interval is displayed literally | N/A yet | No time-range UI exists (docs/AUDIT.md §5 row 44). Owning phase F. |

---

## Out-of-scope confirmation (Phase M)

Confirm none of the following were implemented, and record where any pre-existing instance was found during Phase A:

SSO / per-user OAuth · long-term log storage · SIEM · alerting · full APM · log mutation · cross-source single query · application database · cluster-wide permissions · query audit persistence · HA / horizontal scale · saved or team queries · retention / DR · scheduled queries · tracing-backend integration · pseudonymized lookup service · multi-cluster queries · penetration testing / production approval · AI root-cause diagnosis · analytics · production identity features.

**Phase A finding:** none of the above were found anywhere in the repository. This is trivially true rather than a validated guarantee: the repository contains no application code at all (see docs/AUDIT.md §2, §7), so there is nothing — in scope or out of scope — for any of these to exist within. Re-confirm this row at Phase M once actual implementation exists to check against.
