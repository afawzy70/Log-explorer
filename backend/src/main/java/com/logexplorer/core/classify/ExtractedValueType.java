package com.logexplorer.core.classify;

/** Optional conversion for an extracted value. A value that cannot be converted is reported as invalid, never guessed. */
public enum ExtractedValueType { STRING, INTEGER, DECIMAL, BOOLEAN }
