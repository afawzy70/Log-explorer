/**
 * Phase A2b placeholder. This page exists only to give the Playwright
 * harness (H4b) a real running app to point at, proving the wiring works
 * before Phase F builds the actual investigation UI. Not product UI -
 * deliberately out of Phase A2b's scope per IMPLEMENTATION_PLAN.md.
 */
export default function App() {
  return (
    <main>
      <h1>Log Explorer</h1>
      <p>Phase A2b placeholder page - proves the frontend dev server and Playwright are wired together.</p>
    </main>
  );
}
